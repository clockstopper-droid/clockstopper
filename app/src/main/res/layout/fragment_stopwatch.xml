<?xml version="1.0" encoding="utf-8"?>
<!--
    fragment_stopwatch.xml
    ──────────────────────
    Layout for StopwatchFragment — the sole destination in the navigation graph.

    Visual hierarchy (top → bottom)
    ────────────────────────────────
    1. tv_elapsed_time    Large centered elapsed-time display  ("MM:SS.cc")
    2. Control row        [ Start/Stop ]  [ Lap ]  [ Reset ]
    3. rv_laps            Scrollable lap list (newest lap first)

    Measurement notes
    ─────────────────
    - The elapsed-time display uses a monospace typeface (monospace) so digits
      don't shift width as the value changes, preventing layout jitter.
    - The control buttons use equal weight inside a LinearLayout row so they
      fill the available horizontal space proportionally.
    - The RecyclerView fills the remaining vertical space via layout_weight=1
      inside the outer vertical LinearLayout.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background"
    android:padding="16dp"
    tools:context=".StopwatchFragment">

    <!-- ── Elapsed-time display ──────────────────────────────────────────── -->

    <TextView
        android:id="@+id/tv_elapsed_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="24dp"
        android:fontFamily="monospace"
        android:gravity="center"
        android:includeFontPadding="false"
        android:text="@string/time_zero"
        android:textColor="@color/elapsed_time_text"
        android:textSize="64sp"
        android:textStyle="bold"
        tools:text="01:23.45" />

    <!-- ── Control button row ─────────────────────────────────────────────── -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="16dp">

        <!-- Start / Stop toggle -->
        <Button
            android:id="@+id/btn_start_stop"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:text="@string/btn_start"
            android:textAllCaps="false" />

        <!-- Lap — disabled until the stopwatch is running -->
        <Button
            android:id="@+id/btn_lap"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:enabled="false"
            android:text="@string/btn_lap"
            android:textAllCaps="false" />

        <!-- Reset -->
        <Button
            android:id="@+id/btn_reset"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:text="@string/btn_reset"
            android:textAllCaps="false" />

    </LinearLayout>

    <!-- ── Lap list ───────────────────────────────────────────────────────── -->

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_laps"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingTop="4dp"
        android:paddingBottom="8dp"
        android:scrollbars="vertical"
        tools:listitem="@layout/item_lap" />

</LinearLayout>
